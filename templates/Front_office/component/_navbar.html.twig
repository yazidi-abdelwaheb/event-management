<nav class="navbar navbar-expand-lg" style="background:#1a1a2e; padding: 0 1.5rem; height: 64px;">
  <div class="container-fluid px-0">

    <a class="navbar-brand d-flex align-items-center gap-2" href="{{ path('app_home_page') }}"
       style="text-decoration:none;">
      <div style="width:36px; height:36px; background:rgba(255,255,255,0.12);
                  border-radius:8px; display:flex; align-items:center; justify-content:center;">
        <image src="{{ asset('logo.png') }}" width="50" height="50" alt="Logo Event Management Platform">
      </div>
      <span style="color:white; font-size:16px; font-weight:700; letter-spacing:0.3px;">
        Event Management
      </span>
    </a>

    <button class="navbar-toggler border-0" type="button"
            data-bs-toggle="collapse" data-bs-target="#navbarMain"
            style="color:white; box-shadow:none;">
      <i class="bi bi-list" style="font-size:22px; color:white;"></i>
    </button>

    <div class="collapse navbar-collapse" id="navbarMain">

      <ul class="navbar-nav mx-auto gap-1">
        <li class="nav-item">
          <a class="nav-link {{ app.request.attributes.get('_route') == 'app_home_page' ? 'active' : '' }}"
             href="{{ path('app_home_page') }}">
            Home
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link {{ app.request.attributes.get('_route') starts with 'app_public_event' ? 'active' : '' }}"
             href="{{ path('app_public_event') }}">
            Events
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link {{ app.request.attributes.get('_route') starts with 'app_category' ? 'active' : '' }}"
             href="{{ path('app_category') }}">
            Categories
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Contact</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">About</a>
        </li>
      </ul>

      <div class="d-flex align-items-center gap-2">

        {% if app.user %}
          <div class="dropdown">
            <button class="btn d-flex align-items-center gap-2 dropdown-toggle"
                    type="button" data-bs-toggle="dropdown"
                    style="background:rgba(255,255,255,0.1); border:1px solid rgba(255,255,255,0.2);
                           border-radius:8px; color:white; font-size:14px; padding:6px 12px;">
              <div style="width:28px; height:28px; background:white; border-radius:50%;
                          display:flex; align-items:center; justify-content:center;">
                <span style="font-size:12px; font-weight:700; color:#1a1a2e;">
                  {{ app.user.firstName is defined
                    ? app.user.firstName|first|upper
                    : app.user.userIdentifier|first|upper }}
                </span>
              </div>
              <span>
                {{ app.user.firstName is defined
                  ? app.user.firstName
                  : app.user.userIdentifier }}
              </span>
            </button>
            <ul class="dropdown-menu dropdown-menu-end mt-2"
                style="border-radius:12px; border:1px solid #e5e7eb; box-shadow:0 8px 24px rgba(0,0,0,0.1); min-width:200px;">
              <li>
                <div class="px-3 py-2" style="border-bottom:1px solid #f3f4f6;">
                  <p style="font-size:13px; font-weight:600; color:#1a1a2e; margin:0;">
                    {{ app.user.firstName is defined
                      ? app.user.firstName ~ ' ' ~ app.user.lastName
                      : app.user.userIdentifier }}
                  </p>
                  <p style="font-size:12px; color:#6b7280; margin:0;">{{ app.user.userIdentifier }}</p>
                </div>
              </li>
              <li>
                <a class="dropdown-item d-flex align-items-center gap-2 py-2" href="#"
                   style="font-size:13px; color:#374151;">
                  <i class="bi bi-person" style="font-size:14px;"></i> My Profile
                </a>
              </li>
              <li>
                <a class="dropdown-item d-flex align-items-center gap-2 py-2" href="#"
                   style="font-size:13px; color:#374151;">
                  <i class="bi bi-ticket" style="font-size:14px;"></i> My Tickets
                </a>
              </li>
              <li><hr class="dropdown-divider my-1"></li>
              <li>
                <a class="dropdown-item d-flex align-items-center gap-2 py-2"
                   href="{{ logout_path() }}"
                   style="font-size:13px; color:#dc2626;">
                  <i class="bi bi-box-arrow-right" style="font-size:14px;"></i> Sign Out
                </a>
              </li>
            </ul>
          </div>

        {% else %}
          <a href="{{ path('app_login') }}"
             style="color:rgba(255,255,255,0.85); text-decoration:none; font-size:14px;
                    padding:8px 16px; border-radius:8px; border:1px solid rgba(255,255,255,0.2);">
            Sign In
          </a>
          <a href="{{ path('app_register') }}"
             style="color:#1a1a2e; text-decoration:none; font-size:14px; font-weight:600;
                    padding:8px 16px; border-radius:8px; background:white;">
            Sign Up
          </a>
        {% endif %}

      </div>
    </div>
  </div>
</nav>

<style>
.nav-link {
    color: rgba(255,255,255,0.65) !important;
    font-size: 14px;
    padding: 8px 14px !important;
    border-radius: 8px;
    transition: background 0.2s, color 0.2s;
}
.nav-link:hover, .nav-link.active {
    color: rgba(255,255,255,0.95) !important;
    background: rgba(255,255,255,0.1);
}
.dropdown-item:hover {
    background: #f8f7f4;
    color: #1a1a2e !important;
}
</style>